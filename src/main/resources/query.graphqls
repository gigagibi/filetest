type Post {
    id: ID!,
    name: String,
    desc: String,
    content: String
}

type Query {
    posts:[Post!]!
    post(id: ID):Post
}

type Mutation {
    createPost (name: String!, desc: String!, content: String!): [Post]
}